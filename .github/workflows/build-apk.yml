name: Build Android APK

# Manual only while testing — no automatic builds on push.
# Trigger from Actions → Build Android APK → Run workflow when you want a new APK.
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.32.5"
          channel: stable
          cache: true

      - run: flutter pub get

      - run: flutter analyze

      - run: flutter test

      - run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: expiry-check-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30
